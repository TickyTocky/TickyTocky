import { ICON } from './importImage';
const { nav } = ICON;

export const NAV_HOME_STATUS = {
  default: {
    icon: nav.default.url,
    alt: nav.default.alt,
  },
  active: {
    icon: nav.active.url,
    alt: nav.active.alt,
  },
};
